var assert = require('assert');

describe('Cities Table', function() {
    describe('Table does not exists', function() {
        it('should return empty array', function() {
            assert.equal([], );
        })
    });
});
